{
  "schemaVersion": 1,
  "id": "njpatel.omabot",
  "name": "Omabot",
  "version": "1.0.0",
  "author": "Neil Patel",
  "license": "Apache-2.0",
  "description": "Your Grok Bot roster in the bar: every bot drawn as its own avatar, with an expression for its state and one keypress to the app.",
  "preview": "preview.png",
  "kinds": ["bar-widget"],
  "activation": "on-demand",
  "entryPoints": {
    "barWidget": "Widget.qml"
  },
  "barWidget": {
    "displayName": "Omabot",
    "description": "Bots waiting on you appear in the bar as their own avatars - the shape and colour they have in Grok Bot - and the panel lists every bot by channel with its last message.",
    "category": "AI",
    "aliases": ["omabot", "grok", "grokbot", "bots"],
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "barMetric": "attention",
      "groupBySection": "true",
      "maxBarAvatars": 3
    },
    "schema": [
      {
        "key": "barMetric",
        "type": "enum",
        "label": "What the bar shows",
        "options": ["attention", "all", "count", "none"],
        "defaultValue": "attention",
        "description": "attention = avatars of bots waiting on you or with unread messages, plus their counts; all = also the bot count when nothing needs you; count = the number of bots only; none = no avatars."
      },
      {
        "key": "groupBySection",
        "type": "boolean",
        "label": "Group the panel by channel",
        "defaultValue": "true",
        "description": "Use the channels (sidebar sections) you set up in Grok Bot. Off sorts every bot together, most urgent first."
      },
      {
        "key": "maxBarAvatars",
        "type": "integer",
        "label": "Most avatars to show in the bar",
        "min": 1,
        "max": 6,
        "step": 1,
        "defaultValue": 3
      }
    ]
  }
}
